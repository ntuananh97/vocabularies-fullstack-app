{
    "version": 2,
    "builds": [
        {
            "src": "api/src/server.js",
            "use": "@vercel/node"
        },
        {
            "src": "ui/package.json",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "api/src/server.js" }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}
